services:
  api:
    deploy:
      resources:
        limits:
          memory: 256M
    build: 
      context: ../../
      dockerfile: ./apps/api/docker/Dockerfile
    env_file:
      - .env
    ports:
      - 3000:3000
    depends_on:
      db:
        condition: service_healthy

